import { Text, View } from 'react-native';
import { styles } from '../styles/mainStyles';

export default function ExtrasPage() {
    return (
        <View style={styles.container}>
            <Text>Extras Page</Text>
        </View>
    );
}