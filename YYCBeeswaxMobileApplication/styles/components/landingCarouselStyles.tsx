import { StyleSheet } from "react-native";

export const dividerStyles = StyleSheet.create({
    optionView: {
        backgroundColor: "gray",
        height: 1,
        marginVertical: 10,
    },
});
